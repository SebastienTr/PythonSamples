#! /usr/bin/env python3
# -*- coding: utf-8 -*-

my_variable = True

# We don't have to use { } even ( ), the indentation is enough

if my_variable is True:
	for i in range(0, 2):
		print ("Hello World")
